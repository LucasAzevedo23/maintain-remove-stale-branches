name: Test 

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test maintain-remove-stale-branches
        uses: LucasAzevedo23/maintain-remove-stale-branches@7f814752ed73064608e2a41329f30eb9bf0a5f8d
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          developer_team_slug: developer_team_slug
          admin_team_slug: admin_team_slug